coverage:
  status:
    project:
      default:
        target: 70
        threshold: 1
    patch:
      default:
        target: auto
comment:
  layout: "reach, diff, flags"
  behavior: default
ignore:
  - LogoWallpaperUITests/**
